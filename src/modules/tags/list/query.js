// import gql from 'graphql-tag';

/*
const tagFragment = gql`
  fragment tagFields on Tag {
    id
    name
    description
    color
  }
`;

export const productTagsQuery = gql`
  query {
    viewer {
      productTags {
        ...tagFields
      }
    }
  }
  ${tagFragment}
`;

export const shipmentTagsQuery = gql`
  query {
    viewer {
      shipmentTags {
        ...tagFields
      }
    }
  }
  ${tagFragment}
`;

export const userTagsQuery = gql`
  query {
    viewer {
      userTags {
        ...tagFields
      }
    }
  }
  ${tagFragment}
`;

export const batchTagsQuery = gql`
  query {
    viewer {
      batchTags {
        ...tagFields
      }
    }
  }
  ${tagFragment}
`;

export const requestTagsQuery = gql`
  query {
    viewer {
      requestTags {
        ...tagFields
      }
    }
  }
  ${tagFragment}
`;
*/
