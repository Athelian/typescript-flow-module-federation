// @flow
import { defineMessages } from 'react-intl';

export default defineMessages({
  notLiveUpdated: {
    id: 'modules.sheet.common.notLiveUpdated',
    defaultMessage: 'Not live updated',
  },
});
