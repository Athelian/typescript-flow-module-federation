// @flow
import { css } from 'react-emotion';
import { layout } from 'styles/common';

const OrderFormWrapperStyle = css`
  ${layout.GRID_VERTICAL};
  grid-gap: 40px;
  padding: 50px 0;
`;

export default OrderFormWrapperStyle;
