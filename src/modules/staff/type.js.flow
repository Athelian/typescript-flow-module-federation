// @flow
import type { Tag } from 'components/Tag/type.js.flow';

export type User = {
  id: string,
  firstName: string,
  lastName: string,
  email: string,
  roles: Array<{
    id: string,
    name: string,
  }>,
  tags: Array<Tag>,
  createdAt: string,
  updatedAt: string,
  organization: ?{
    name: string,
  },
};
