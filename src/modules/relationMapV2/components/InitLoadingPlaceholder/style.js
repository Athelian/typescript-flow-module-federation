// @flow
import { css } from 'react-emotion';

export const InitLoadingPlaceholderWrapperStyle = (width: number): string => css`
  width: ${width}px;
`;

export default InitLoadingPlaceholderWrapperStyle;
