// @flow
import { css } from 'react-emotion';
import { fontSizes, presets, colors } from 'styles/common';

export const RadiosWrapperStyle = css`
  ${fontSizes.MAIN};
  ${presets.ELLIPSIS};
  color: ${colors.BLACK};
`;
