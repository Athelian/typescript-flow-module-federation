// @flow
import { colors } from 'styles/common';

export const colorPresets = [
  colors.PURPLE,
  colors.BLUE,
  colors.TEAL,
  colors.YELLOW,
  colors.ORANGE,
  colors.RED,
  colors.GRAY_LIGHT,
  colors.GRAY_DARK,
  colors.BLACK,
  colors.GRAY_SUPER_LIGHT,
];

export default colorPresets;
