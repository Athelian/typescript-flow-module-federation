// @flow

export type TaskCardEditableProps = {
  name: boolean,
  startDate: boolean,
  dueDate: boolean,
  inProgress: boolean,
  skipped: boolean,
  completed: boolean,
  approved: boolean,
  rejected: boolean,
};
